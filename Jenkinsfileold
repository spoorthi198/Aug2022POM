pipeline{

    agent any

    stages{

        stage("Build"){
            steps{
                echo("Build")
            }
        }

          stage("Run utiliteis"){
            steps{
                echo("Run utilities")
            }
        }

        stage("deploy to Dev"){
            steps{
                echo("deploy to Dev")
            }
        }

     stage("deploy to QA"){
            steps{
                echo("deploy to QA")
            }
        }

     stage("Run Automation"){
            steps{
                echo("Run Automation")
            }
        }

     stage("deploy to stage"){
            steps{
                echo("deploy to stage")
            }
        }
     stage("deploy to uat"){
            steps{
                echo("deploy to uat")
            }
        }
     stage("deploy to prod"){
            steps{
                echo("deploy to prod")
            }
        }
    }
}